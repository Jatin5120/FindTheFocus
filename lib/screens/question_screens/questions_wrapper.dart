import 'package:find_the_focus/controllers/controllers.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

class QuestionsWrapper extends StatelessWidget {
  const QuestionsWrapper({Key? key}) : super(key: key);

  static QuestionsController questionsController = Get.find();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
